import React, { useState } from 'react';

const Dashboard = () => {
  // State management for the active tab to keep cognitive load low
  const [activeTab, setActiveTab] = useState('morning');

  return (
    <div className="min-h-screen bg-slate-50 font-sans text-slate-800">
      {/* Top Navigation Bar */}
      <header className="bg-white shadow-sm px-6 py-4 flex justify-between items-center">
        <div className="flex items-center space-x-2">
          <span className="text-2xl font-bold text-indigo-600">Kindred Coach</span>
        </div>
        <div className="flex items-center space-x-4">
          {/* Security Indicator */}
          <span className="text-xs font-semibold text-emerald-600 bg-emerald-50 px-3 py-1 rounded-full flex items-center">
             <svg className="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path></svg>
             Passkey Secured
          </span>
          <div className="w-10 h-10 bg-indigo-100 rounded-full flex items-center justify-center text-indigo-700 font-bold">
            U
          </div>
        </div>
      </header>

      {/* Main Content Area */}
      <main className="max-w-5xl mx-auto py-8 px-4 sm:px-6 lg:px-8">
        
        {/* Tab Navigation */}
        <div className="flex space-x-4 mb-8 border-b border-slate-200">
          <button 
            onClick={() => setActiveTab('morning')}
            className={`pb-4 px-2 font-medium text-sm transition-colors ${activeTab === 'morning' ? 'border-b-2 border-indigo-600 text-indigo-600' : 'text-slate-500 hover:text-slate-700'}`}
          >
            Morning Baseline
          </button>
          <button 
            onClick={() => setActiveTab('scans')}
            className={`pb-4 px-2 font-medium text-sm transition-colors ${activeTab === 'scans' ? 'border-b-2 border-indigo-600 text-indigo-600' : 'text-slate-500 hover:text-slate-700'}`}
          >
            Interval Scans
          </button>
          <button 
            onClick={() => setActiveTab('evening')}
            className={`pb-4 px-2 font-medium text-sm transition-colors ${activeTab === 'evening' ? 'border-b-2 border-indigo-600 text-indigo-600' : 'text-slate-500 hover:text-slate-700'}`}
          >
            Evening Report
          </button>
          <button 
            onClick={() => setActiveTab('goals')}
            className={`pb-4 px-2 font-medium text-sm transition-colors ${activeTab === 'goals' ? 'border-b-2 border-indigo-600 text-indigo-600' : 'text-slate-500 hover:text-slate-700'}`}
          >
            Goals & Habits
          </button>
        </div>

        {/* Dynamic Content Rendering */}
        <div className="bg-white rounded-xl shadow-sm border border-slate-100 p-6 md:p-8">
          
          {activeTab === 'morning' && (
            <div className="space-y-6">
              <h2 className="text-xl font-bold text-slate-800">Morning Assessment</h2>
              <p className="text-slate-500 text-sm">Let's establish your baseline for the day without any judgment.</p>
              
              {/* Mental Load Scan */}
              <div className="bg-slate-50 p-5 rounded-lg border border-slate-100">
                <h3 className="font-semibold mb-3">Current Mental Load (Withdraw Dimensions)</h3>
                <select className="w-full p-3 rounded border border-slate-200 bg-white focus:ring-2 focus:ring-indigo-500 outline-none">
                  <option value="">Select your current capacity...</option>
                  <option value="clear">Clear / Flow State</option>
                  <option value="mild">Mild Preoccupation (Background Noise)</option>
                  <option value="moderate">Moderate Preoccupation (Functional but Strained)</option>
                  <option value="enormous">Enormous Obsession (Total Impairment)</option>
                </select>
              </div>

              {/* SMART Mini-Goals */}
              <div className="bg-slate-50 p-5 rounded-lg border border-slate-100">
                <h3 className="font-semibold mb-3">Daily Mini-Goals (SMART)</h3>
                <input type="text" placeholder="1. E.g., Complete one cycle of laundry" className="w-full p-3 mb-2 rounded border border-slate-200 outline-none" />
                <input type="text" placeholder="2. E.g., Read one page of research" className="w-full p-3 mb-2 rounded border border-slate-200 outline-none" />
                <input type="text" placeholder="3. E.g., Hydrate" className="w-full p-3 rounded border border-slate-200 outline-none" />
              </div>
              
              <button className="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-4 rounded transition-colors">
                Save Morning Baseline
              </button>
            </div>
          )}

          {activeTab === 'scans' && (
             <div className="space-y-6">
               <h2 className="text-xl font-bold text-slate-800">Interval Body Scan</h2>
               <p className="text-slate-500 text-sm">Checking in on your physical indications to intercept early triggers.</p>
               {/* Digital Feelings Wheel Placeholder */}
               <div className="flex items-center justify-center h-48 bg-slate-100 rounded-lg border border-dashed border-slate-300">
                  <span className="text-slate-400 font-medium">[ Interactive Feelings Wheel Component ]</span>
               </div>
               <button className="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-4 rounded transition-colors">
                Log Body Scan
              </button>
             </div>
          )}

          {activeTab === 'evening' && (
             <div className="space-y-6">
               <h2 className="text-xl font-bold text-slate-800">Evening Report</h2>
               
               {/* Medication Effectiveness */}
               <div className="bg-slate-50 p-5 rounded-lg border border-slate-100">
                <h3 className="font-semibold mb-3">Medication Effectiveness</h3>
                <div className="flex items-center space-x-4">
                  <span className="text-sm text-slate-500">Not Effective</span>
                  <input type="range" min="1" max="10" defaultValue="5" className="w-full accent-indigo-600" />
                  <span className="text-sm text-slate-500">Highly Effective</span>
                </div>
              </div>

               <button className="w-full bg-indigo-600 hover:bg-indigo-700 text-white font-bold py-3 px-4 rounded transition-colors">
                Generate Coach's Reflective Summary
              </button>
             </div>
          )}

          {activeTab === 'goals' && (
             <div className="space-y-6">
               <h2 className="text-xl font-bold text-slate-800">90-Day Habit Tracker</h2>
               <p className="text-slate-500 text-sm">Longitudinal data to build resilience and consistency.</p>
               <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
                 {/* Active Habit Card */}
                 <div className="border border-slate-200 rounded-lg p-4 bg-white">
                    <h4 className="font-bold text-indigo-600">Morning Reflection</h4>
                    <p className="text-sm text-slate-500 mt-1">Day 14 of 90</p>
                    <div className="w-full bg-slate-200 rounded-full h-2.5 mt-3">
                      <div className="bg-indigo-600 h-2.5 rounded-full" style={{ width: '15%' }}></div>
                    </div>
                 </div>
               </div>
             </div>
          )}

        </div>
      </main>
    </div>
  );
};

export default Dashboard;
