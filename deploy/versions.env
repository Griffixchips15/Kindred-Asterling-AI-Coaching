# This repository's tested version set. Renovate these together after staging tests.
KARPENTER_VERSION=1.8.2
KEDA_VERSION=2.18.2
METRICS_SERVER_VERSION=3.12.2

# Replace with an alias returned by `aws ec2 describe-images`/Karpenter AMI
# discovery for the cluster minor, and promote it through staging. Example syntax:
BOTTLEROCKET_AMI_ALIAS=bottlerocket@latest
