1. We need to extract the sidebar logic from `AppLayout` into a separate component called `AppSidebar`.
2. I will create `src/components/layout/app-sidebar.tsx`.
3. The `AppSidebar` component will receive props like `collapsed`, `setCollapsed`, `location`, `user`, and `logout`. Or better, it can internally call `useLocation`, `useAuth`, and `useSidebarCollapsed`. It will be cleaner if it manages its own state or uses those hooks. Actually, let's keep it simple: `AppSidebar` can use `useLocation`, `useAuth`, and `useSidebarCollapsed` directly, just like `AppLayout` does currently.
4. The `AppLayout` component will then just render the `<AppSidebar />` and the `<main>` content.
5. Wait, `useLocation`, `useAuth`, `useSidebarCollapsed` can all be called inside `AppSidebar`.
6. Let's look at `AppLayout` again. It renders `<div className="flex h-screen bg-background text-foreground overflow-hidden">` and inside it, `<aside>` and `<main>`.
7. I will move the whole `<aside>` and all the things only used by it (like `navItems`, `ProfilePanel`, `NavLinkItem`, `ThemePicker`, `useSidebarCollapsed`, etc.) into `app-sidebar.tsx`.
8. Wait, looking at `app-layout.tsx`, all those things are in this file right now.
   - `useSidebarCollapsed`
   - `ThemePicker`
   - `navItems`
   - `ProfilePanel`
   - `safeFormatDate`
   - `NavLinkItem`
   These are ALL only used for the Sidebar. So I can move almost the entire `app-layout.tsx` into `app-sidebar.tsx`, except for the `AppLayout` component itself, which will just import `AppSidebar` and wrap `{children}`.
9. Wait, if I do that, is there any reason to keep them in `app-layout.tsx`? No. The issue specifically states: "Extracting the sidebar logic into its own component would make the layout easier to manage."
