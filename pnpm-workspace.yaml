# ============================================================================
# SECURITY: Minimum release age for npm packages (supply-chain attack defense)
# ============================================================================
#
# This setting requires that any npm package version must have been published
# for at least 1 day (1440 minutes) before pnpm will allow installing it.
# This is a critical defense against supply-chain attacks. In most cases,
# malicious npm releases are discovered and pulled within hours, so a 1-day
# delay provides a strong safety buffer.
#
# DO NOT DISABLE THIS SETTING. Removing or setting it to 0 is considered
# extremely dangerous and leaves the entire workspace vulnerable to supply-
# chain attacks, which have been the #1 vector for npm ecosystem compromises.
#
# If you absolutely need to install a package before the 1-day window has
# passed (e.g. an urgent security bugfix), you can add it to the
# `minimumReleaseAgeExclude` allowlist below. Only consider doing this for
# packages released by trusted organizations with an impeccable security
# posture (e.g. Replit packsges, react from Meta, typescript from Microsoft). Even then,
# remove the exclusion once the 1-day window has passed.
#
# Example:
#   minimumReleaseAgeExclude:
#     - react
#     - typescript
#
# ============================================================================
minimumReleaseAge: 1440

minimumReleaseAgeExclude:
  # Exclude @replit scoped packages from the minimum release age check.
  # These are published by Replit and trusted — the supply-chain attack vector
  # this setting guards against does not apply to our own packages.
  - '@replit/*'

packages:
  - artifacts/*
  - lib/*
  - lib/integrations/*
  - scripts

catalog:
  '@replit/vite-plugin-cartographer': ^0.5.1
  '@replit/vite-plugin-dev-banner': ^0.1.1
  '@replit/vite-plugin-runtime-error-modal': ^0.0.6
  '@tailwindcss/vite': ^4.1.14
  '@tanstack/react-query': ^5.90.21
  '@types/node': ^25.3.3
  '@types/react': ^19.2.0
  '@types/react-dom': ^19.2.0
  '@vitejs/plugin-react': ^5.0.4
  class-variance-authority: ^0.7.1
  clsx: ^2.1.1
  drizzle-orm: ^0.45.2
  framer-motion: ^12.23.24
  lucide-react: ^0.545.0
  # Must be this exact version because expo requires it
  react: 19.1.0
  # Must be this exact version because expo requires it
  react-dom: 19.1.0
  tailwind-merge: ^3.3.1
  tailwindcss: ^4.1.14
  tsx: ^4.21.0
  vite: ^7.3.5
  wouter: ^3.3.5
  zod: ^3.25.76
  vitest: ^4.1.7

autoInstallPeers: false

onlyBuiltDependencies:
  - '@google/genai'
  - esbuild

overrides:
  # =====================================================
  # SECURITY PATCH OVERRIDES
  # =====================================================
  # These force specific versions of transitive dependencies
  # to fix known CVEs without waiting for the parent package
  # to bump its own dependency range.

  # GHSA-6v5v-wf23-fmfq: markdown-it quadratic-complexity DoS
  markdown-it: ">=14.2.0"

  # GHSA-h67p-54hq-rp68: js-yaml quadratic-complexity DoS
  js-yaml: ">=5.2.2"

  # GHSA-f38q-mgvj-vph7: protobufjs shadow-property prototype pollution
  protobufjs: ">=8.6.6"

  # GHSA-4x5r-pxfx-6jf8: @babel/core arbitrary file read via sourceMappingURL
  "@babel/core": ">=7.29.6"

  # GHSA-fx2h-pf6j-xcff / GHSA-v6wh-96g9-6wx3: vite directory traversal
  vite: ">=7.3.5"

  # GHSA-q8mj-m7cp-5q26: qs DoS via stringify with comma arrays
  qs: ">=6.15.2"

  # GHSA-7r4r-j53v-m7jg: body-parser DoS via large URL-encoded payload
  body-parser: ">=2.3.0"

  # GHSA-5fm2-v9c9-c5mg: brace-expansion ReDoS
  brace-expansion: ">=5.0.8"

  # GHSA-j8xg-fqg8-7prw / GHSA-g78m-2chm-r7qv: linkify-it ReDoS
  linkify-it: ">=5.0.2"

  # GHSA-2wrf-vwv3-j5mr: fast-uri ReDoS
  fast-uri: ">=3.1.4"

  # GHSA-45r3-m6w4-7fc2: postcss ReDoS
  postcss: ">=8.5.18"

  # drizzle-kit uses esbuild internally on a vulnerable version
  esbuild: ">=0.28.1"
  "@esbuild-kit/esm-loader": "npm:tsx@^4.21.0"

allowBuilds:
  '@google/genai': true
  esbuild: true
